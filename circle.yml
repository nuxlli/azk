machine:
  services:
    - docker
dependencies:
  pre:
    - bash src/libexec/pre_circle.sh
  override:
    # Install azk dependencies
    - make dependencies
    - make
    - docker pull azukiapp/azktcl:0.0.2
test:
  override:
    - ./bin/azk agent start
    - ./bin/azk nvm grunt slow_test
